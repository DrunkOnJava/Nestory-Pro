// Tests.xcconfig
// Test target-specific build settings
// Created for v1.1 - P1-01

// Note: Tests intentionally do NOT set SWIFT_DEFAULT_ACTOR_ISOLATION
// because XCTestCase methods (setUp, tearDown) are nonisolated
// and conflict with MainActor default isolation

// MARK: - Swift Settings (No MainActor default for tests)
SWIFT_VERSION = 6.0
SWIFT_STRICT_CONCURRENCY = complete
SWIFT_ENFORCE_EXCLUSIVE_ACCESS = full
SWIFT_APPROACHABLE_CONCURRENCY = YES
// CRITICAL: Explicitly set nonisolated to override project-level MainActor default
// XCTestCase lifecycle methods (init, setUp, tearDown) are nonisolated and conflict with MainActor
SWIFT_DEFAULT_ACTOR_ISOLATION = nonisolated

// MARK: - Thread Safety (enabled for tests)
// TODO: Re-enable TSan after fixing Swift 6 concurrency compatibility
// TSan crashes on simulator with "tsan.module_ctor" error
ENABLE_THREAD_SANITIZER = NO
ENABLE_MAIN_THREAD_CHECKER = YES

// MARK: - String Catalogs
STRING_CATALOG_GENERATE_SYMBOLS = NO
